# Codex Plan Usage

Codex is used in two ways:

1. **TDD implementer** — one phase at a time, writing failing tests first.
2. **Adversarial reviewer** — after the plan and after each phase, comparing rewrite behavior to original source evidence.

Codex must be prompted with exact phase file, source evidence, and acceptance commands. It must not declare parity from a single green slice.
